package config

const (
	// DBName for database name
	DBName string = "coba"
	// DBPort for database connection port
	DBPort string = "5432"
	// DBUser for database user
	DBUser string = "postgres"
	// DBPass for database password
	DBPass string = "test100"
	// DBHost for database host
	DBHost string = "localhost"
)
